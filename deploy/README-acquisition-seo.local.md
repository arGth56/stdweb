# Branch `local/acquisition-seo` (local only)

This branch adds **optional** acquisition / SEO (`ACQUISITION_SEO_ENABLED` in `.env`), similar to the ObsBS footer (`HOSTING_*`).

**Do not push** this branch to `origin` unless you want SEO in the shared repository.

Operator notes and gpu-3 deploy helpers live in **`local/acquisition/`** (gitignored).
